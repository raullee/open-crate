## What & why

<!-- What does this change, and what problem does it solve? -->

## Checklist

- [ ] `corepack pnpm test` passes
- [ ] `corepack pnpm build` passes
- [ ] Engine changes stay zero-dependency and framework-agnostic
- [ ] Local-first / no lock-in respected (no mandatory account, no phone-home)
- [ ] Schema changes are optional + `OCF_VERSION` bumped if breaking
- [ ] Docs updated if behaviour changed

## Notes

<!-- Anything reviewers should know. New generator/adapter? Link the spec. -->
